package pl.devmentoring.uruchomieniepierwszegoprogramu;

public class Zad1 {

    //Zad. 1 [Interview]
    //Odpowiedz na pytanie, czy Java jest językiem kompilowanym czy interpretowanym?

    // Kompilacja: Java jest językiem kompilowanym jak i interpretowanym. Jest to główny powód dlaczego Java jest tak wszechstronna.
    //Java jest językiem kompilowanym, ponieważ kod źródłowy (pliki .java) jest kompilowany przez kompilator Javy (javac) do postaci kodu bajtowego (pliki .class).
    // Kod bajtowy jest tłumaczony na kod maszynowy za pomocą JVM.
    //Interpretacja: Po skompilowaniu, kod bajtowy Java nie jest wykonywany bezpośrednio przez system operacyjny, zamiast tego jest uruchamiany przez JVM.
    //JIT – Kompilacja JIT jest procesem, gdzie kod bajtowy jest kompilowany do natywnego kodu maszynowego w locie,
    // co oznacza, że konwersja ta ma miejsce w trakcie działania programu. Jest to optymalizacja, która pozwala na szybsze wykonanie programów Java,
    // ponieważ często używane fragmenty kodu są przekształcane w szybko działający kod maszynowy, unikając potrzeby wielokrotnego interpretowania tego samego kodu.
}
