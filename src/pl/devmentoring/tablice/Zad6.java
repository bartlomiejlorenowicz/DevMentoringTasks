package pl.devmentoring.tablice;

public class Zad6 {
    // Czym jest błąd (wyjątek) java.lang.ArrayIndexOutOfBoundsException i kiedy występuje?

    // java.lang.ArrayIndexOutOfBoundsException jest wyjątkiem, który występuje gdy programista odwołuje się do indeksu,
    // który nie istnieje (indeks jest większy od rozmiaru tablicy lub mniejszy ujemny).
}
